/* ---------- FAQ ---------- */
.faq{margin-top:48px}
.faq:first-of-type{margin-top:8px}
.faq-cat{
  display:block;font-size:11.5px;font-weight:700;letter-spacing:.18em;text-transform:uppercase;
  color:var(--orange);margin:0 0 18px;
}
.faq details{
  border-top:1px solid var(--line);background:transparent;
}
.faq details:last-of-type{border-bottom:1px solid var(--line)}
.faq summary{
  cursor:pointer;list-style:none;padding:22px 44px 22px 0;position:relative;
  font-family:Figtree,sans-serif;font-size:clamp(17px,1.9vw,20px);font-weight:600;color:var(--white);
  line-height:1.4;transition:color .18s ease;
}
.faq summary::-webkit-details-marker{display:none}
.faq summary:hover{color:var(--orange)}
.faq summary::after{
  content:"";position:absolute;right:8px;top:calc(50% - 1px);
  width:15px;height:2px;background:var(--orange);transition:transform .22s ease;
}
.faq summary::before{
  content:"";position:absolute;right:8px;top:calc(50% - 1px);
  width:15px;height:2px;background:var(--orange);transform:rotate(90deg);transition:transform .22s ease,opacity .22s ease;
}
.faq details[open] summary::before{transform:rotate(0deg);opacity:0}
.faq .faq-a{padding:0 0 26px;max-width:68ch}
.faq .faq-a p{color:var(--grey);font-size:16.5px;line-height:1.65;margin:0 0 14px}
.faq .faq-a p:last-child{margin-bottom:0}
.faq .faq-a a{color:var(--orange);text-decoration:none;border-bottom:1px solid rgba(255,108,44,.4)}
.faq .faq-a a:hover{border-bottom-color:var(--orange)}
.faq summary:focus-visible{outline:2px solid var(--orange);outline-offset:3px}
@media (prefers-reduced-motion:reduce){.faq summary::after,.faq summary::before{transition:none}}
